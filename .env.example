# Flight API Configuration
# Replace with your actual API keys from https://developers.amadeus.com

# Amadeus API (for real flight data)
AMADEUS_API_KEY=************************
AMADEUS_API_SECRET=****************

# Server Configuration
NODE_ENV=development
PORT=3000

# Note: 
# 1. Create a free account at https://developers.amadeus.com
# 2. Create a new application to get your API credentials
# 3. Copy your API Key and Secret to replace the asterisks above
# 4. The server will automatically fall back to mock data if credentials are invalid